<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context="team.udacity.uos.doodle.activity.LoginActivity">

    <com.facebook.widget.LoginButton
        android:id="@+id/login_facebook_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="@string/facebook_login_text"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Map Activity"
        android:id="@+id/button_map"
        android:layout_above="@+id/login_facebook_btn"
        android:layout_alignRight="@+id/login_facebook_btn"
        android:layout_alignEnd="@+id/login_facebook_btn"
        android:layout_marginBottom="77dp" />


</RelativeLayout>
